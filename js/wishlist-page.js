import {fetchProducts} from './products.js';import {getWishlist} from './wishlist.js';import {renderProducts} from './app.js';(async()=>{const all=await fetchProducts();const ids=getWishlist();const a=all.filter(p=>ids.includes(p.id));const g=document.querySelector('[data-wishlist-grid]');if(!a.length)g.outerHTML='<div class="empty">Your wishlist is empty.<br><br><a class="button" href="shop.html">Explore the collection</a></div>';else renderProducts(g,a)})();