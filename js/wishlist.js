const KEY='vc_wishlist'; export function getWishlist(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}} export function toggleWishlist(id){let a=getWishlist();a=a.includes(id)?a.filter(x=>x!==id):[...a,id];localStorage.setItem(KEY,JSON.stringify(a));document.dispatchEvent(new CustomEvent('wishlist:updated'));return a.includes(id)} export const isWishlisted=id=>getWishlist().includes(id); export const wishlistCount=()=>getWishlist().length;
